import * as React from "react";

import { Button } from "@mijn-ui/react-button";

const page = () => {
	return (
		<div className="text-red-500">
			<Button>MijnUI Button</Button>
		</div>
	);
};

export default page;
